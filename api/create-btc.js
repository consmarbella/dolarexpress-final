export default async function handler(req, res) {   res.setHeader('Access-Control-Allow-Origin', '*');   res.setHeader('Access-Control-Allow-Methods', 'POST, OPTIONS');   res.setHeader('Access-Control-Allow-Headers', 'Content-Type');    if (req.method === 'OPTIONS') return res.status(200).end();   if (req.method !== 'POST') return res.status(405).json({ error: 'Method not allowed' });    let from_amount, email, rawBody;   try {     const buffers = [];     for await (const chunk of req) buffers.push(chunk);     rawBody = Buffer.concat(buffers).toString();     const body = JSON.parse(rawBody || '{}');     from_amount = body.from_amount;     email = body.email;   } catch (e) {     return res.status(400).json({ error: 'Body invalido', debug: e.message, raw: rawBody });   }    if (!from_amount || from_amount < 20) {     return res.status(400).json({ error: 'Monto minimo 20 USD' });   }    try {     const ip = req.headers['x-forwarded-for'] || req.socket.remoteAddress;     const headers = {       'Content-Type': 'application/json',       'Accept': 'application/json',       'x-api-key': 'aba29bc3-2e85-481c-8e62-f409d0561684'     };     if (ip) headers['x-forwarded-for'] = ip;      const response = await fetch('https://api-payments.guardarian.com/v1/transaction?customer_country=CL', {       method: 'POST',       headers,       body: JSON.stringify({         from_amount: from_amount,         from_currency: 'USD',         to_currency: 'BTC',         from_network: 'USD',         to_network: 'BTC',         deposit: {           payment_category: 'VISA_MC',           skip_choose_payment_category: true         },         payout_info: {           payout_address: '14GjaVeCyQwXLxSmVhMi9tbmXDCFt1G2Zd',           skip_choose_payout_address: true         },         customer: {           contact_info: { email: email || 'cliente@dolarexpress.cl' }         },         redirects: {           successful: 'https://dolarexpress.cl/',           cancelled: 'https://dolarexpress.cl/',           failed: 'https://dolarexpress.cl/'         },         locale: 'es'       })     });      const data = await response.json();      if (!response.ok) {       return res.status(400).json({ error: data.message || data.error || 'Error al crear transaccion' });     }      const tid = data.id;     const authToken = data.preauth_token;     const checkoutUrl = authToken       ? `https://payments.guardarian.com/checkout?tid=${tid}&auth_token=${authToken}&locale=es`       : `https://payments.guardarian.com/checkout?tid=${tid}&locale=es`;      res.json({ url: checkoutUrl });   } catch (error) {     res.status(500).json({ error: error.message });   } }
